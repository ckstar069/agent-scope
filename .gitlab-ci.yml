stages:
  - verify
  - build
  - release

variables:
  NODE_VERSION: "20"
  CARGO_HOME: "$CI_PROJECT_DIR/.cargo"
  npm_config_cache: "$CI_PROJECT_DIR/.npm"
  PLAYWRIGHT_BROWSERS_PATH: "$CI_PROJECT_DIR/.cache/ms-playwright"
  CARGO_NET_RETRY: "3"
  CARGO_HTTP_TIMEOUT: "120"

# 缓存依赖目录，避免重复下载
# 评估结论：保留全部 4 个目录；ms-playwright 缓存浏览器文件价值最高
cache:
  key: ${CI_COMMIT_REF_SLUG}
  paths:
    - .npm/
    - .cargo/registry/
    - .cargo/git/
    - .cache/ms-playwright/

# -----------------------------------------------------------------------------
# Verify Stage: 代码检查、测试（Push / MR 时触发）
# -----------------------------------------------------------------------------

verify:
  stage: verify
  image: agent-scope-ci:node20-rust1.95
  tags:
    - linux
  before_script:
    - echo "=== 安装前端依赖 ==="
    - npm ci
    - echo "=== 安装 Playwright 浏览器 ==="
    - npx playwright install chromium
  script:
    - echo "=== 构建前端 ==="
    - npm run build
    - echo "=== Rust 格式化检查 ==="
    - cd "$CI_PROJECT_DIR/src-tauri" && cargo fmt --check
    - echo "=== Rust Clippy 检查 ==="
    - cd "$CI_PROJECT_DIR/src-tauri" && cargo clippy -- -D warnings
    - echo "=== Rust 检查 ==="
    - cd "$CI_PROJECT_DIR/src-tauri" && cargo check
    - echo "=== Rust 测试 ==="
    - cd "$CI_PROJECT_DIR/src-tauri" && cargo test
    - echo "=== Rust 安全审计 ==="
    - cd "$CI_PROJECT_DIR/src-tauri" && cargo audit || echo "cargo audit 发现警告（非阻塞）"
    - echo "=== npm 安全审计 ==="
    - npm audit --audit-level=moderate || echo "npm audit 发现警告（非阻塞）"
    - echo "=== E2E 测试 ==="
    - npm test
  artifacts:
    when: always
    paths:
      - .sisyphus/evidence/e2e/report/
    expire_in: 1 week
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "main"'

# -----------------------------------------------------------------------------
# Build Stage: 桌面应用构建（Tag Push 时触发）
# -----------------------------------------------------------------------------

# 版本号同步脚本：将 Git tag (vX.Y.Z) 同步到 package.json 和 Cargo.toml
.sync-version: &sync-version-script
  - |
    VERSION=${CI_COMMIT_TAG#v}
    echo "Tag: $CI_COMMIT_TAG -> Version: $VERSION"
    # 更新 package.json
    node -e "const fs=require('fs'); const f='package.json'; const p=JSON.parse(fs.readFileSync(f)); p.version='$VERSION'; fs.writeFileSync(f, JSON.stringify(p,null,2)+'\n'); console.log('Updated', f, '->', p.version);"
    # 更新 Cargo.toml
    sed -i "s/^version = \".*\"/version = \"$VERSION\"/" src-tauri/Cargo.toml
    echo "Updated src-tauri/Cargo.toml -> $VERSION"
    # tauri.conf.json 已配置为读取 package.json 版本号，无需额外修改

build:linux:
  stage: build
  image: agent-scope-ci:node20-rust1.95
  tags:
    - linux
  variables:
    CARGO_HOME: "$CI_PROJECT_DIR/.cargo"
    npm_config_cache: "$CI_PROJECT_DIR/.npm"
    APPIMAGE_EXTRACT_AND_RUN: "1"
  cache:
    key: ${CI_COMMIT_REF_SLUG}
    paths:
      - .npm/
      - .cargo/registry/
      - .cargo/git/
  before_script:
    - echo "=== 同步版本号 ==="
    - *sync-version-script
    - echo "=== 安装前端依赖 ==="
    - npm ci
    - echo "=== 安装 cargo-tauri CLI ==="
    - cargo binstall tauri-cli --locked --no-confirm
  script:
    - echo "=== 构建前端 ==="
    - npm run build
    - echo "=== 构建 Linux 桌面应用 ==="
    - cd "$CI_PROJECT_DIR/src-tauri" && cargo tauri build --target x86_64-unknown-linux-gnu
  artifacts:
    when: on_success
    paths:
      - src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/deb/*.deb
      - src-tauri/target/x86_64-unknown-linux-gnu/release/bundle/appimage/*.AppImage
    expire_in: 1 month
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+(-[0-9A-Za-z.-]+)?$/'

build:windows:
  stage: build
  tags:
    - windows
  variables:
    CARGO_HOME: "$CI_PROJECT_DIR\\.cargo"
    npm_config_cache: "$CI_PROJECT_DIR\\.npm"
  cache:
    key: ${CI_COMMIT_REF_SLUG}-windows
    paths:
      - .npm/
      - .cargo/registry/
      - .cargo/git/
  before_script:
    - echo "=== 同步版本号 ==="
    - |
      $VERSION = $env:CI_COMMIT_TAG -replace '^v',''
      Write-Host "Tag: $env:CI_COMMIT_TAG -> Version: $VERSION"
      node -e "const fs=require('fs'); const f='package.json'; const p=JSON.parse(fs.readFileSync(f)); p.version='$VERSION'; fs.writeFileSync(f, JSON.stringify(p,null,2)+'\n'); console.log('Updated', f, '->', p.version);"
      (Get-Content src-tauri/Cargo.toml -Raw) -replace '^version = ".*"', "version = `"$VERSION`"" | Set-Content src-tauri/Cargo.toml -NoNewline
      Write-Host "Updated src-tauri/Cargo.toml -> $VERSION"
    - echo "=== 安装前端依赖 ==="
    - npm ci
  script:
    - echo "=== 构建前端 ==="
    - npm run build
    - echo "=== 构建 Windows 桌面应用 ==="
    - cd src-tauri
    - cargo tauri build --target x86_64-pc-windows-msvc
    - echo "=== 打包 Portable 免安装版 ==="
    - |
      $VERSION = $env:CI_COMMIT_TAG -replace '^v',''
      $TARGET = "x86_64-pc-windows-msvc"
      $RELEASE_DIR = "target/$TARGET/release"
      $ZIP_NAME = "AgentScope_${VERSION}_x64_portable.zip"
      # 进入 release 目录打包 exe（Windows 上 Tauri 默认静态链接，无需额外 dll）
      Compress-Archive -Path "$RELEASE_DIR/agent-scope.exe" -DestinationPath "$RELEASE_DIR/$ZIP_NAME" -Force
      Write-Host "Created portable: $RELEASE_DIR/$ZIP_NAME"
      # 移动 zip 到 bundle 目录，方便 artifact 统一收集
      New-Item -ItemType Directory -Path "target/$TARGET/release/bundle/portable" -Force | Out-Null
      Move-Item -Path "$RELEASE_DIR/$ZIP_NAME" -Destination "target/$TARGET/release/bundle/portable/$ZIP_NAME" -Force
  artifacts:
    when: on_success
    paths:
      - src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe
      - src-tauri/target/x86_64-pc-windows-msvc/release/bundle/portable/*.zip
    expire_in: 1 month
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+(-[0-9A-Za-z.-]+)?$/'

# -----------------------------------------------------------------------------
# Release Stage: 创建 GitLab Release 并汇总产物
# -----------------------------------------------------------------------------

release:
  stage: release
  image: alpine:latest
  tags:
    - linux
  needs:
    - job: build:linux
      artifacts: true
    - job: build:windows
      artifacts: true
  script:
    - apk add --no-cache curl jq
    - echo "=== 调试：当前目录和文件 ==="
    - pwd && ls -la
    - echo "=== 调试：查找所有产物 ==="
    - find . -name "*.deb" -o -name "*.AppImage" -o -name "*.exe" -o -name "*.zip" | head -20
    - echo "=== 收集产物 ==="
    - |
      DEB=$(find . -name "*.deb" | head -1)
      APPIMAGE=$(find . -name "*.AppImage" | head -1)
      EXE=$(find . -name "*.exe" | head -1)
      ZIP=$(find . -name "*.zip" | head -1)

      echo "deb: $DEB"
      echo "appimage: $APPIMAGE"
      echo "exe: $EXE"
      echo "zip: $ZIP"
    - echo "=== 创建 GitLab Release ==="
    - |
      DEB=$(find . -name "*.deb" | head -1)
      APPIMAGE=$(find . -name "*.AppImage" | head -1)
      EXE=$(find . -name "*.exe" | head -1)
      ZIP=$(find . -name "*.zip" | head -1)

      JOB_URL="${CI_SERVER_URL}/${CI_PROJECT_PATH}/-/jobs/${CI_JOB_ID}"

      # 构建 Release 描述（用 printf 确保 \n 变成真正的换行符）
      DESC=$(printf '## AgentScope %s\n\n### Linux\n' "${CI_COMMIT_TAG}")
      [ -n "$DEB" ] && DESC=$(printf '%s- deb: `%s`\n' "$DESC" "$(basename "$DEB")")
      [ -n "$APPIMAGE" ] && DESC=$(printf '%s- AppImage (免安装): `%s`\n' "$DESC" "$(basename "$APPIMAGE")")
      DESC=$(printf '%s\n### Windows\n' "$DESC")
      [ -n "$EXE" ] && DESC=$(printf '%s- Installer: `%s`\n' "$DESC" "$(basename "$EXE")")
      [ -n "$ZIP" ] && DESC=$(printf '%s- Portable (免安装): `%s`\n' "$DESC" "$(basename "$ZIP")")
      DESC=$(printf '%s\n---\n点击下方 Artifacts 区域下载各平台安装包。' "$DESC")

      # 构建 assets links
      LINKS="[]"
      [ -n "$DEB" ] && LINKS=$(echo "$LINKS" | jq ". + [{\"name\":\"Linux deb\",\"url\":\"${JOB_URL}/artifacts/raw/${DEB}\",\"link_type\":\"package\"}]")
      [ -n "$APPIMAGE" ] && LINKS=$(echo "$LINKS" | jq ". + [{\"name\":\"Linux AppImage\",\"url\":\"${JOB_URL}/artifacts/raw/${APPIMAGE}\",\"link_type\":\"package\"}]")
      [ -n "$EXE" ] && LINKS=$(echo "$LINKS" | jq ". + [{\"name\":\"Windows Installer\",\"url\":\"${JOB_URL}/artifacts/raw/${EXE}\",\"link_type\":\"package\"}]")
      [ -n "$ZIP" ] && LINKS=$(echo "$LINKS" | jq ". + [{\"name\":\"Windows Portable\",\"url\":\"${JOB_URL}/artifacts/raw/${ZIP}\",\"link_type\":\"package\"}]")

      # 创建 Release（-k 跳过自签名证书验证）
      curl -k --fail --request POST \
        --header "JOB-TOKEN: ${CI_JOB_TOKEN}" \
        --header "Content-Type: application/json" \
        --data "{
          \"name\": \"AgentScope ${CI_COMMIT_TAG}\",
          \"tag_name\": \"${CI_COMMIT_TAG}\",
          \"description\": $(echo -n "$DESC" | jq -sR .),
          \"assets\": { \"links\": ${LINKS} }
        }" \
        "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/releases"

      echo "Release created successfully"
  artifacts:
    paths:
      - "**/*.deb"
      - "**/*.AppImage"
      - "**/*.exe"
      - "**/*.zip"
    expire_in: 1 month
  rules:
    - if: '$CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/'
